import 'package:flutter/material.dart';

class ReAppbar extends StatelessWidget {
  const ReAppbar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
